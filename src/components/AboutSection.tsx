export default function AboutSection() {
  return (
    <section id="about" className="bg-[#17201a] py-[60px] text-white">
      <div className="mx-auto max-w-6xl px-6 max-sm:px-4">
        <h2 className="text-[38px] leading-[1.15] font-black">
          What this project proves
        </h2>
        <p className="mt-4 max-w-[820px] leading-8 text-white/75">
          You can explain the folder structure, edit a page, split UI into
          components, reuse data with map, and run the app locally. That is a
          strong first Next.js task.
        </p>
      </div>
    </section>
  );
}
